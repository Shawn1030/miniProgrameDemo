
<view class="outer" bindtap="outerClick" capture-bind:tap="outerCapture">
  <view class="middle" bind:tap="middleClick" capture-bind:tap="middleCapture">
    <view class="inner"  mut-bind:tap="innerClick" capture-bind:tap="innerCapture"></view>
  </view>
</view>

